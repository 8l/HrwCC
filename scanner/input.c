
//Include this little file in
#include "input.h"

/**This is after include*/
typedef struct mystruct
{
		int a;
		int b;
};


struct mystruct* _myinstance2;


struct mystruct *myfunc(int a,void *argv)
{
		const c;

		while( (a<= b && b >=a) || !(a==b) && a != b)
		{
				i.b = 3;
				break;
		}


		c = (a%b) & (a|b);
		c = c^ ~a->s;

		
		return argv[2]/(myinstance->b && -55);
}




void main( int argc, char** argv)
{
		if( argc > 1 || argc<8)
		{
			printf(      "hello world.\n");
			continue;
		}
		else
			printf(      "hello \"world??\n");

		return sizeof(-1 + 'a\'');
}


