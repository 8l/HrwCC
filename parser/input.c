

int* mytestfunction( int arg1, mystruct arg3, char a);


/*Does this work?*/


int* mytestfunction( int arg1, mystruct arg3)
{
		return 0;
}

ä
struct mystruct
{
		int a;
		test();
		int** b;
		struct mystruct* c;
};

int global;
void** global2;


#
#include "input.h"
char global3;
#include "unknown.h"

void main()
{
		int a;
		int b;
		char** c[4];
		mystruct* h;

		if( (*var.a->b->c[2] <= *var->test[func(func(2) + 2) - 3]  ||  seven+3*w-8) && a)
		{
				*aa = *aa + 1;

				while( notempty > sizeof(mytype**))
						return -4 & bitfield;

				test();
				continue
				break;

				int* a;


				return;;

				else
					test();

				if( not )
					die()

				if( -(*var1->test[5+5] + ~var2*22 - myfunc(nested() + 2))*'h' == sizeof(int**) 
					||  ! sizeof(char*) <= sizeof(mystruct) + "hello" 
					&& 2 | &n & a[2] != var.a->b[2] || 2==3 )
					happy(flag);

		
		else
		{
				print("nothing to do %d\n", i);
		}
}

void func3()
{



struct mytype
{
		test();
};

void func3()
{
}


struct mytype
{
		test();
		int* void;
};

